对两个有序数组先进行排序，再找到中位数，因为都是有序数组，所以排序的时间复杂度为O(log(m + n))
因为这是不等长的数组，如果是两个长度相等的数组，可以采用下面的方法：
（1）找到两个数组的中位数，并比较，如果两个中位数相等，则直接返回该值，否则：
如果mid1>mid2，则数组1返回它的前半部分，数组2返回它的后半部分，形成两个新的数组，这两个新的数组等长，并重复（1）
如果mid1<mid2，则数组1返回它的后半部分，数组2返回它的前半部分，形成两个新的数组，这两个新的数组等长，并重复（1）
