对于整数x, 每次对10求余数，得到的就是最后一位的数r，然后x等于x除以10
把这个数存储在数组p中,并用count记录下数组的长度
p[count] = r
count++
x = x/10

最终数组p中存储的就是反转之后的数字，在把这些数字组合成一个整数：
for (i=0;i<count-1;i++)
num = p[i] * pow(10, count-1-i)


特此要注意的是越界问题，当反转之前或者反转之后的数越界了，要直接返回0